#include <stdio.h>

int main()
{
	int x;
	int y;
	int z;
	int average;

	printf("정수를 입력하시오 :");
	scanf_s("%d", &x);

	printf("정수를 입력하시오 :");
	scanf_s("%d", &y);

	printf("정수를 입력하시오 :");
	scanf_s("%d", &z);

	average = (x + y + z) / 3;
	printf("평균은 %d 입니다.", average);

	return 0;
}
